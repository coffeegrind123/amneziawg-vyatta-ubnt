--- a/lib/c-stack.c
+++ b/lib/c-stack.c
@@ -52,7 +52,8 @@
 #if DEBUG
 # include <stdio.h>
 #endif
-#elif HAVE_LIBSIGSEGV && SIGSTKSZ < 16384
+/* Fix for glibc where SIGSTKSZ is not a compile-time constant */
+#elif HAVE_LIBSIGSEGV && (defined SIGSTKSZ ? SIGSTKSZ : 16384) < 16384
 
 /* libsigsegv 2.6 through 2.8 have a bug where some architectures use
    more than the Linux default of an 8k alternate stack when deciding
